# Frontend (Vercel) — copier vers les variables d'environnement du projet Vercel

# URL de l'API backend sur Render (obligatoire en production)
NEXT_PUBLIC_API_URL=https://votre-service.onrender.com/api

# UploadThing (optionnel, pour les images)
UPLOADTHING_TOKEN=

# Routes API Next.js legacy (optionnel si vous n'utilisez que le backend Render)
# MONGODB_URI=
