# ─── Frontend (Next.js) — .env.local ───────────────────────────────────────
# Uniquement des variables exposées au navigateur (NEXT_PUBLIC_*).

NEXT_PUBLIC_API_URL=http://localhost:5000/api

UPLOADTHING_TOKEN=

# ─── Backend — backend/.env ─────────────────────────────────────────────────
# Voir backend/.env.example (JWT_SECRET, MongoDB, CinetPay, etc.)
